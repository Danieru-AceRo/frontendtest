
##  Scripts disponibles


### `npm start`

Corre la app en modo desarrollo.\
abrir [http://localhost:3000](http://localhost:3000) para verlo en el navegador.



### `npm run build`

Hace un builds de la app para producción en el folder`build` .\
Empaqueta correctamente React en modo de producción y optimiza la compilación para obtener el mejor rendimiento.

El build se minimiza y los nombres de archivo incluyen los hashes.\
La app está disponible para deploy



Este proyecto Requiere npm, disponible en [https://www.npmjs.com/]